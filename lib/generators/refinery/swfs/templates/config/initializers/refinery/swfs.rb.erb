Refinery::Swfs.configure do |config|
    #Configures the maximum allowed upload size (in bytes) for an swf file
    #config.max_file_size = <%= Refinery::Swfs.max_file_size %>

    # Configure how many swfs per page should be displayed in the list of images in the admin area
    #config.pages_per_admin_index =  <%= Refinery::Swfs.pages_per_admin_index %>

    # Configure white-listed mime types for validation
    #config.whitelisted_mime_types = <%= Refinery::Swfs.whitelisted_mime_types %>

    # Configure Dragonfly
    # This is where in the middleware stack to insert the Dragonfly middleware
    # config.dragonfly_insert_before = <%= Refinery::Swfs.dragonfly_insert_before %>
    # config.dragonfly_secret = <%= Refinery::Swfs.dragonfly_secret %>
    # If you decide to trust file extensions replace :ext below with :format
    # config.dragonfly_url_format = <%= Refinery::Swfs.dragonfly_url_format %>
    # config.datastore_root_path = <%= Refinery::Swfs.datastore_root_path %>
    # config.trust_file_extensions = <%= Refinery::Swfs.trust_file_extensions %>

end
